spring:
  mvc:
    static-path-pattern: /static/**
    view:
      prefix: classpath:/templates/
      suffix: .html
  application:
    name: grpc-client
server:
  port: 8888
  tomcat:
    uri-encoding: utf-8
registry:
  servers: 127.0.0.1:2181
  nodepath: /grpc_ke